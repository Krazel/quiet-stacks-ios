const test=require('node:test'),assert=require('node:assert/strict'),{create}=require('../web/js/gallery-book-motion.js');
test('lift and settle preserve aspect ratio and return exactly to the hitbox',()=>{let t=0;const m=create({now:()=>t,reduced:()=>false}),r={x:10,y:20,w:12,h:27};m.lift(3);t=65;assert.equal(m.tick(),true);const raised=m.rect(3,r,2);assert.ok(raised.y<r.y);assert.equal(raised.w/raised.h,r.w/r.h);t=140;assert.equal(m.tick(),false);m.release(3);t=240;m.tick();assert.notDeepEqual(m.rect(3,r,2),r);t=331;assert.equal(m.tick(),false);assert.deepEqual(m.rect(3,r,2),r);});
test('cancel, rapid repick and reduced motion leave no queued animations',()=>{let t=0,reduced=false;const m=create({now:()=>t,reduced:()=>reduced}),r={x:0,y:0,w:9,h:27};for(let i=0;i<20;i++){m.lift(i);t+=10;m.release(i,true);}assert.equal(m.stats().settling,4);m.lift(19);assert.equal(m.stats().settling,3);reduced=true;assert.equal(m.tick(),false);assert.deepEqual(m.rect(19,r,8),r);m.clear();assert.equal(m.stats().held,null);});
